[package]
name = "jump"
version = "0.1.0"
description = "Jump helps you navigate faster by learning your habits"
homepage = "http://gsamokovarov.com/jump/"
license = "MIT"
url = "https://github.com/vicvenscob/jump/archive/refs/heads/main.tar.gz"
depends = []
post_install = "if [ -f jump ]; then cp jump /usr/local/bin/jump; else go build -o /usr/local/bin/jump; fi"

[package.files]
